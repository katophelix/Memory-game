import React from "react";
import "./Header.css";

const Header = props => (
    <header className="header">
    <h3>Click on an image to earn points, but don't click any images more than once!</h3>
    </header>
)
export default Header;
